//in the backend if we set type to module we have use import and when we import the module we have to use .js extension
import React from 'react'

function Chat() {
  return (
    <div>
      CHAT
    </div>
  )
}

export default Chat
